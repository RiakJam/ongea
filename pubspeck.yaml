name: ongea
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.8.1

dependencies:
  flutter:
    sdk: flutter
  video_player: ^2.10.0
  cupertino_icons: ^1.0.8
  image_picker: ^1.0.4
  camera: ^0.11.2
  path_provider: ^2.1.2
  permission_handler: ^12.0.1
  visibility_detector: ^0.4.0+2
  
  firebase_core: ^4.0.0
  firebase_auth: ^6.0.1
  firebase_database: ^12.0.0
  firebase_storage: ^13.0.0
  cloud_firestore: ^6.0.0  
  
  font_awesome_flutter: ^10.7.0
  media_kit_video: ^1.1.8
  media_kit_libs_linux: ^1.2.1 # For Linux support o
  video_player_web: ^2.0.20
  url_launcher: ^6.1.11
  country_picker: ^2.0.20
  country_code_picker: ^3.0.0
  in_app_purchase: ^3.1.7  # Working version
  shared_preferences: ^2.2.3
  http: ^1.4.0
  intl: 0.20.2
  country_pickers: ^3.0.1 # For country data
  # google_mobile_ads: ^10.5.0 
  device_info_plus: ^11.5.0
  pay: ^3.2.1
  media_kit: ^1.2.0


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0
  flutter_launcher_icons: ^0.14.4

flutter:
  uses-material-design: true

  assets:
    - assets/icon.png

flutter_icons:
  android: true
  ios: true
  image_path: "assets/icon.png"
  background_color: "#000000"
